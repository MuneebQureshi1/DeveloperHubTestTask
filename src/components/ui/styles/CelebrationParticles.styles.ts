import { StyleSheet } from 'react-native';

export const celebrationParticlesStyles = StyleSheet.create({
  container: {
    ...StyleSheet.absoluteFill,
    alignItems: 'center',
    justifyContent: 'flex-start',
    paddingTop: '22%',
  },
  particle: {
    position: 'absolute',
  },
});
