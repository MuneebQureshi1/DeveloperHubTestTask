import { StyleSheet } from 'react-native';

export const EMOJI_SIZE = 64;

export const styles = StyleSheet.create({
  emoji: {
    fontSize: EMOJI_SIZE,
    left: 0,
    position: 'absolute',
    top: 0,
  },
});
